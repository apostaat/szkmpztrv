{
  "name": "szkmpztrv",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^6.1.4",
    "@testing-library/react": "^14.1.2",
    "@testing-library/user-event": "^14.5.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.8.1",
    "react-scripts": "5.0.1",
    "p5": "^1.7.0",
    "web-vitals": "^3.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:low-memory": "NODE_OPTIONS='--max-old-space-size=2048' react-scripts build",
    "build:minimal": "NODE_OPTIONS='--max-old-space-size=1024' react-scripts build",
    "build:ultra-minimal": "NODE_OPTIONS='--max-old-space-size=512' react-scripts build",
    "build:extreme-minimal": "NODE_OPTIONS='--max-old-space-size=256' react-scripts build",
    "build:production": "GENERATE_SOURCEMAP=false react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "overrides": {
    "inflight": "^1.0.6",
    "eslint": "^8.57.1"
  }
}
